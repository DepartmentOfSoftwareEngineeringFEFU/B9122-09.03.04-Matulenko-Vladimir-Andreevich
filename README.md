# Визуализация природных явления в 3D-картах

Дипломный проект — клиент-серверное веб-приложение для генерации интерактивного 3D-рельефа (с автоматической загрузкой карт высот из AWS S3 Terrarium) и наложения метеорологических данных (землетрясения, туман, ветер) с гео-проекцией координат.

## Технологический стек

| Компонент | Технологии |
|-----------|-----------|
| **Backend** | Python, FastAPI, SQLAlchemy (SQLite), aiohttp |
| **Frontend** | React, Three.js (@react-three/fiber), Tailwind CSS |
| **Сборщик** | Vite |

## Структура проекта

```
Diplom/
├── backend/
│   ├── main.py            # API-эндпоинты FastAPI
│   ├── database.py        # Подключение к БД SQLite
│   ├── models.py          # Модели таблиц (SimulationState, WindStation, Earthquake)
│   ├── schemas.py         # Pydantic-схемы валидации
│   ├── weather_service.py # Сервис загрузки погоды с Open-Meteo
│   ├── terrain_service.py # Сервис скачивания тайлов высот AWS S3
│   └── requirements.txt   # Python-зависимости
├── frontend/
│   ├── src/
│   │   ├── App.jsx              # Главный компонент приложения
│   │   ├── components/
│   │   │   ├── Terrain.jsx      # 3D-рельеф (PlaneGeometry + heightmap)
│   │   │   ├── WeatherEffects.jsx # Визуализация тумана и землетрясений
│   │   │   ├── WindSystem.jsx   # Система частиц потоков ветра (IDW-интерполяция)
│   │   │   └── Sidebar.jsx      # Панель управления и менеджер слоёв
│   │   └── utils/
│   │       └── geo.js           # Математика гео-проекции (Lat/Lon → X/Z)
│   ├── index.html
│   └── package.json
└── tests/
    └── ...                    # Тестовые конфигурации метеоданных
```

## Быстрый старт

### Требования

- **Python** 3.10+
- **Node.js** 18+
- **npm** (устанавливается вместе с Node.js)

### 1. Клонирование репозитория

```bash
git clone https://github.com/ВАШ_ЛОГИН/Diplom.git
cd Diplom
```

### 2. Настройка Backend

```bash
# Переходим в папку бэкенда
cd backend

# Создаём виртуальное окружение
python -m venv .venv

# Активируем его (Windows PowerShell)
.\.venv\Scripts\activate
# Для Linux/macOS: source .venv/bin/activate

# Устанавливаем зависимости
pip install -r requirements.txt
```

### 3. Настройка Frontend

```bash
# Открываем новый терминал
cd frontend
npm install
```

### 4. Запуск (нужны два терминала)

**Терминал 1 — Backend (FastAPI):**
```bash
cd backend
# Убедитесь, что виртуальное окружение активировано
uvicorn main:app --reload
```
Сервер запустится на `http://127.0.0.1:8000`

**Терминал 2 — Frontend (React):**
```bash
cd frontend
npm run dev
```
Приложение откроется на `http://localhost:5173`

### 5. Использование

1. Откройте `http://localhost:5173` в браузере. По умолчанию приложение откроется на координатах г. Владивосток и загрузит актуальный ветер с Open-Meteo.
2. Для изменения локации загрузите JSON-файл симуляции (содержащий координаты, параметры тумана, землетрясений и кастомного ветра) через кнопку **«Метеоданные (JSON)»** в панели слева.
   - *Примеры файлов находятся в папке `tests/` (например, `tests/exp_extreme_weather.json`).*
3. После загрузки JSON, бэкенд автоматически скачает нужный тайл рельефа (AWS S3 Terrarium) по указанным координатам и передаст его на клиент.
4. Управляйте видимостью слоёв (рельеф, туман, ветер, землетрясения) через чекбоксы.
5. Настройте вертикальный масштаб (Z-Scale) для лучшей видимости перепадов высот.
6. Вращайте 3D-сцену мышкой (ЛКМ), перемещайте (ПКМ), масштабируйте колёсиком.

## API-документация

После запуска бэкенда доступна автогенерируемая документация:
- **Swagger UI:** `http://127.0.0.1:8000/docs`
- **ReDoc:** `http://127.0.0.1:8000/redoc`

## Лицензия

Учебный проект. Все права защищены.
